# AWS deployment

This repo is no longer the active deployment source for `ryleyreid.com`.

## GitHub configuration

The live site now deploys from `scroll-cv-showcase`.

Keep this repo as code/history only unless you intentionally rewire the AWS deployment target.
